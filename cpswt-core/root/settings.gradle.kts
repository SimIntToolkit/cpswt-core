rootProject.name = "root"

pluginManagement {
    repositories {
        gradlePluginPortal()
    }
    plugins {
    }
}

dependencyResolutionManagement {
    repositories {
        mavenLocal()
        mavenCentral()
    }
}

//includeBuild("../base-events")
//includeBuild("../config")
//includeBuild("../root")
//includeBuild("../utils")
